<?php
$this->pageBreadcrumbs()->setItems([
    'Main' => $this->url('admin'),
    'Users' => $this->url('admin', ['action' => 'users']),
    'Change password for ' . $this->escapeHtml($user->getEmail()) => $this->url('admin', ['action' => 'changePassword'])
]);
?>
<?= $this->partial('application/user/change-password'); ?>